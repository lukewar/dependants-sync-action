{
  "name": "dependants-sync-action",
  "version": "1.0.0",
  "description": "Automates the synchronization of fields in GitHub Projects between parent issues and their dependents.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "ncc build src/dependants-sync.js -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "@vercel/ncc": "^0.33.0"
  }
}
